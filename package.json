{
  "name": "plotlist",
  "version": "0.1.0",
  "private": true,
  "description": "PlotList — the UK allotment directory: find available plots, waiting-list times and how to apply, council by council.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test data/*.test.ts"
  },
  "dependencies": {
    "next": "^16.2.9",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.7.2"
  }
}
